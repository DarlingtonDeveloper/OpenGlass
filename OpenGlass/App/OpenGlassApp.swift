// OpenGlass - OpenGlassApp.swift

import SwiftUI

/// Main app entry point for OpenGlass.
/// TODO: Initialize services (Gemini, OpenClaw, Camera) on launch
/// TODO: Set up environment objects for dependency injection
/// TODO: Handle app lifecycle (background/foreground reconnection)
@main
struct OpenGlassApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
